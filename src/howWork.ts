// first we need to install typescript using----npm install -g typescript
// then in html we can add js file with a folder like output casue where we will get all compile file there because typescript work ulimatly on js. it covert that in js
// then we can create a tsconfig.json file using --- tsc --init to more controll.
//  also for run we can use only tsc after setting our src and ouput. but we can use -w to watch live
